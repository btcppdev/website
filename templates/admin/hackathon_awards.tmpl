<!doctype html>
<html lang="en">
<head>
  <title>{{ .Competition.Title }} Awards - Admin - bitcoin++</title>
  {{ block "header" . }} {{ end }}
  {{ template "admin_favicon" "" }}
</head>
<body class="bg-gray-50 text-gray-900">
  {{ block "mainnav" . }} {{ end }}
  <div class="mx-auto max-w-6xl px-4 py-8 sm:px-6 lg:px-8">
    <div class="mb-6 flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">
      <div>
        <a href="/admin/hackathons" class="text-sm font-medium text-indigo-600 hover:text-indigo-500">&larr; Hackathons</a>
        <h1 class="mt-2 text-2xl font-semibold tracking-tight">{{ .Competition.Title }} awards</h1>
        <p class="mt-1 text-sm text-gray-600">Set up awards and prizes before assigning winners.</p>
      </div>
      <div class="flex flex-wrap gap-2">
        <a href="{{ .ProjectsURL .Competition }}" class="inline-flex items-center justify-center rounded-md bg-white px-3 py-2 text-sm font-semibold text-gray-900 shadow-sm ring-1 ring-gray-300 hover:bg-gray-50">Projects</a>
        <a href="{{ .ScoreReviewURL .Competition }}" class="inline-flex items-center justify-center rounded-md bg-white px-3 py-2 text-sm font-semibold text-gray-900 shadow-sm ring-1 ring-gray-300 hover:bg-gray-50">Scores</a>
        <a href="{{ .JudgingURL .Competition }}" class="inline-flex items-center justify-center rounded-md bg-white px-3 py-2 text-sm font-semibold text-gray-900 shadow-sm ring-1 ring-gray-300 hover:bg-gray-50">Judging</a>
      </div>
    </div>

    {{ if .FlashMessage }}
    <div class="mb-4 rounded-md border border-green-200 bg-green-50 px-4 py-3 text-sm text-green-800">{{ .FlashMessage }}</div>
    {{ end }}
    {{ if .FlashError }}
    <div class="mb-4 rounded-md border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-800">{{ .FlashError }}</div>
    {{ end }}

    <div class="grid gap-6 lg:grid-cols-[1fr_24rem]">
      <section class="overflow-hidden rounded-lg bg-white shadow">
        <div class="border-b border-gray-200 px-4 py-4 sm:px-6">
          <div class="flex flex-wrap items-center justify-between gap-3">
            <h2 class="text-sm font-semibold text-gray-900">Awards</h2>
            <span class="text-sm text-gray-500">{{ len .Awards }} configured</span>
          </div>
        </div>
        <div class="px-4 py-5 sm:p-6">
          {{ if .Awards }}
          <div class="space-y-4">
            {{ range .Awards }}
            <article class="rounded-md border border-gray-200 px-4 py-4">
              <div class="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between">
                <div>
                  <h3 class="text-base font-semibold text-gray-900">{{ .Title }}</h3>
                  {{ if .Description }}
                  <p class="mt-1 whitespace-pre-line text-sm text-gray-600">{{ .Description }}</p>
                  {{ end }}
                  <div class="mt-3 flex flex-wrap gap-2">
                    <span class="rounded-full bg-gray-100 px-2 py-0.5 text-xs font-medium text-gray-700">{{ $.AwardStatusLabel .Status }}</span>
                    <span class="rounded-full bg-gray-100 px-2 py-0.5 text-xs font-medium text-gray-700">{{ $.OptionalIntLabel .MaxAwardees }} awardees</span>
                    {{ if .OptInRequired }}
                    <span class="rounded-full bg-indigo-50 px-2 py-0.5 text-xs font-medium text-indigo-700">Opt-in required</span>
                    {{ end }}
                  </div>
                </div>
              </div>

              {{ $awardees := $.Awardees . }}
              <div class="mt-4 border-t border-gray-100 pt-4">
                <h4 class="text-sm font-semibold text-gray-900">Winners</h4>
                {{ if $awardees }}
                <ul class="mt-2 divide-y divide-gray-100 rounded-md border border-gray-200">
                  {{ range $awardees }}
                  <li class="flex flex-col gap-3 px-3 py-3 text-sm sm:flex-row sm:items-center sm:justify-between">
                    <div>
                      <div class="font-medium text-gray-900">{{ .ProjectTitle }}</div>
                      <div class="mt-1 text-xs text-gray-500">#{{ $.ProjectAwardNumber . }} · awarded {{ .AwardedAt.Format "Jan 2, 2006 3:04 PM" }}</div>
                    </div>
                    <form method="POST" action="{{ $.AwardsURL $.Competition }}/remove">
                      <input type="hidden" name="AwardID" value="{{ .AwardID }}">
                      <input type="hidden" name="ProjectID" value="{{ .ProjectID }}">
                      <button type="submit" class="rounded-md px-2.5 py-1.5 text-xs font-semibold text-red-700 hover:bg-red-50">Remove</button>
                    </form>
                  </li>
                  {{ end }}
                </ul>
                {{ else }}
                <p class="mt-2 text-sm text-gray-600">No winners assigned yet.</p>
                {{ end }}

                <form method="POST" action="{{ $.AwardsURL $.Competition }}/assign" class="mt-3 grid gap-3 sm:grid-cols-[1fr_auto] sm:items-end">
                  <input type="hidden" name="AwardID" value="{{ .ID }}">
                  <div>
                    <label for="ProjectID-{{ .ID }}" class="block text-xs font-medium text-gray-700">Assign project</label>
                    <select id="ProjectID-{{ .ID }}" name="ProjectID" required
                            class="mt-1 block w-full rounded-md bg-white px-3 py-2 text-sm text-gray-900 outline outline-1 outline-gray-300 focus:outline-2 focus:outline-indigo-600">
                      <option value="">Select project</option>
                      {{ range $.Projects }}
                      <option value="{{ .ID }}">{{ $.ProjectSelectLabel . }}</option>
                      {{ end }}
                    </select>
                  </div>
                  <button type="submit" class="rounded-md bg-indigo-600 px-3 py-2 text-sm font-semibold text-white hover:bg-indigo-700">Assign</button>
                </form>
              </div>

              {{ $prizes := $.AwardPrizes . }}
              <div class="mt-4 border-t border-gray-100 pt-4">
                <h4 class="text-sm font-semibold text-gray-900">Prizes</h4>
                {{ if $prizes }}
                <ul class="mt-2 divide-y divide-gray-100 rounded-md border border-gray-200">
                  {{ range $prizes }}
                  <li class="px-3 py-3 text-sm">
                    <div class="flex flex-col gap-2 sm:flex-row sm:items-start sm:justify-between">
                      <div>
                        <div class="font-medium text-gray-900">{{ .Title }}</div>
                        <div class="mt-1 text-xs text-gray-500">{{ $.PrizeTypeLabel .PrizeType }} · {{ $.PrizeStatusLabel .Status }}{{ if .ValueText }} · {{ .ValueText }}{{ end }}{{ if .PoolPercentage }} · {{ $.PercentLabel .PoolPercentage }} pool{{ end }}</div>
                      </div>
                    </div>
                    {{ if .Description }}<p class="mt-2 text-sm text-gray-600">{{ .Description }}</p>{{ end }}
                    {{ if .PoolURL }}<a href="{{ .PoolURL }}" class="mt-2 inline-block text-sm font-medium text-indigo-600 hover:text-indigo-500">Pool</a>{{ end }}
                  </li>
                  {{ end }}
                </ul>
                {{ else }}
                <p class="mt-2 text-sm text-gray-600">No prizes added yet.</p>
                {{ end }}
              </div>

              <form method="POST" action="{{ $.AwardsURL $.Competition }}/prizes" class="mt-4 grid gap-3 rounded-md bg-gray-50 p-3">
                <input type="hidden" name="AwardID" value="{{ .ID }}">
                <div class="grid gap-3 sm:grid-cols-2">
                  <div>
                    <label for="PrizeTitle-{{ .ID }}" class="block text-xs font-medium text-gray-700">Prize title *</label>
                    <input id="PrizeTitle-{{ .ID }}" name="Title" type="text" required
                           class="mt-1 block w-full rounded-md bg-white px-3 py-2 text-sm text-gray-900 outline outline-1 outline-gray-300 focus:outline-2 focus:outline-indigo-600">
                  </div>
                  <div>
                    <label for="PrizeType-{{ .ID }}" class="block text-xs font-medium text-gray-700">Type *</label>
                    <select id="PrizeType-{{ .ID }}" name="PrizeType" required
                            class="mt-1 block w-full rounded-md bg-white px-3 py-2 text-sm text-gray-900 outline outline-1 outline-gray-300 focus:outline-2 focus:outline-indigo-600">
                      <option value="sats">Sats</option>
                      <option value="in_kind">In-kind</option>
                      <option value="tickets">Tickets</option>
                      <option value="pooled">Pooled</option>
                      <option value="trophy">Trophy</option>
                    </select>
                  </div>
                </div>
                <div class="grid gap-3 sm:grid-cols-3">
                  <div>
                    <label for="ValueText-{{ .ID }}" class="block text-xs font-medium text-gray-700">Value</label>
                    <input id="ValueText-{{ .ID }}" name="ValueText" type="text"
                           class="mt-1 block w-full rounded-md bg-white px-3 py-2 text-sm text-gray-900 outline outline-1 outline-gray-300 focus:outline-2 focus:outline-indigo-600">
                  </div>
                  <div>
                    <label for="PoolPercentage-{{ .ID }}" class="block text-xs font-medium text-gray-700">Pool %</label>
                    <input id="PoolPercentage-{{ .ID }}" name="PoolPercentage" type="number" min="0" max="100" step="0.01"
                           class="mt-1 block w-full rounded-md bg-white px-3 py-2 text-sm text-gray-900 outline outline-1 outline-gray-300 focus:outline-2 focus:outline-indigo-600">
                  </div>
                  <div>
                    <label for="PrizeStatus-{{ .ID }}" class="block text-xs font-medium text-gray-700">Status *</label>
                    <select id="PrizeStatus-{{ .ID }}" name="Status" required
                            class="mt-1 block w-full rounded-md bg-white px-3 py-2 text-sm text-gray-900 outline outline-1 outline-gray-300 focus:outline-2 focus:outline-indigo-600">
                      <option value="available">Available</option>
                      <option value="needs_funds">Needs funds</option>
                      <option value="awarded">Awarded</option>
                      <option value="paid">Paid</option>
                    </select>
                  </div>
                </div>
                <div>
                  <label for="PrizeDescription-{{ .ID }}" class="block text-xs font-medium text-gray-700">Description</label>
                  <textarea id="PrizeDescription-{{ .ID }}" name="Description" rows="2"
                            class="mt-1 block w-full rounded-md bg-white px-3 py-2 text-sm text-gray-900 outline outline-1 outline-gray-300 focus:outline-2 focus:outline-indigo-600"></textarea>
                </div>
                <div class="grid gap-3 sm:grid-cols-2">
                  <div>
                    <label for="PoolURL-{{ .ID }}" class="block text-xs font-medium text-gray-700">Pool URL</label>
                    <input id="PoolURL-{{ .ID }}" name="PoolURL" type="url"
                           class="mt-1 block w-full rounded-md bg-white px-3 py-2 text-sm text-gray-900 outline outline-1 outline-gray-300 focus:outline-2 focus:outline-indigo-600">
                  </div>
                  <div>
                    <label for="Comments-{{ .ID }}" class="block text-xs font-medium text-gray-700">Internal comments</label>
                    <input id="Comments-{{ .ID }}" name="Comments" type="text"
                           class="mt-1 block w-full rounded-md bg-white px-3 py-2 text-sm text-gray-900 outline outline-1 outline-gray-300 focus:outline-2 focus:outline-indigo-600">
                  </div>
                </div>
                <div class="flex justify-end">
                  <button type="submit" class="rounded-md bg-indigo-600 px-3 py-2 text-sm font-semibold text-white hover:bg-indigo-700">Add prize</button>
                </div>
              </form>
            </article>
            {{ end }}
          </div>
          {{ else }}
          <p class="text-sm text-gray-600">No awards configured yet.</p>
          {{ end }}
        </div>
      </section>

      <section class="overflow-hidden rounded-lg bg-white shadow">
        <div class="border-b border-gray-200 px-4 py-4 sm:px-6">
          <h2 class="text-sm font-semibold text-gray-900">Add award</h2>
        </div>
        <form method="POST" action="{{ .AwardsURL .Competition }}" class="grid gap-4 px-4 py-5 sm:p-6">
          <div>
            <label for="Title" class="block text-sm font-medium text-gray-700">Title *</label>
            <input id="Title" name="Title" type="text" required
                   class="mt-1 block w-full rounded-md bg-white px-3 py-2 text-sm text-gray-900 outline outline-1 outline-gray-300 focus:outline-2 focus:outline-indigo-600">
          </div>
          <div>
            <label for="Description" class="block text-sm font-medium text-gray-700">Description</label>
            <textarea id="Description" name="Description" rows="4"
                      class="mt-1 block w-full rounded-md bg-white px-3 py-2 text-sm text-gray-900 outline outline-1 outline-gray-300 focus:outline-2 focus:outline-indigo-600"></textarea>
          </div>
          <div>
            <label for="PhotoURL" class="block text-sm font-medium text-gray-700">Photo URL</label>
            <input id="PhotoURL" name="PhotoURL" type="url"
                   class="mt-1 block w-full rounded-md bg-white px-3 py-2 text-sm text-gray-900 outline outline-1 outline-gray-300 focus:outline-2 focus:outline-indigo-600">
          </div>
          <div class="grid gap-4 sm:grid-cols-2">
            <div>
              <label for="MaxAwardees" class="block text-sm font-medium text-gray-700">Max awardees</label>
              <input id="MaxAwardees" name="MaxAwardees" type="number" min="1"
                     class="mt-1 block w-full rounded-md bg-white px-3 py-2 text-sm text-gray-900 outline outline-1 outline-gray-300 focus:outline-2 focus:outline-indigo-600">
            </div>
            <div>
              <label for="Status" class="block text-sm font-medium text-gray-700">Status *</label>
              <select id="Status" name="Status" required
                      class="mt-1 block w-full rounded-md bg-white px-3 py-2 text-sm text-gray-900 outline outline-1 outline-gray-300 focus:outline-2 focus:outline-indigo-600">
                <option value="draft">Draft</option>
                <option value="available">Available</option>
                <option value="unawarded">Unawarded</option>
                <option value="awarded">Awarded</option>
              </select>
            </div>
          </div>
          <label class="inline-flex items-center gap-2 text-sm text-gray-700">
            <input name="OptInRequired" type="checkbox" class="h-4 w-4 rounded border-gray-300 text-indigo-600 focus:ring-indigo-600">
            Opt-in required
          </label>
          <div class="flex justify-end">
            <button type="submit" class="rounded-md bg-indigo-600 px-3 py-2 text-sm font-semibold text-white hover:bg-indigo-700">Add award</button>
          </div>
        </form>
      </section>
    </div>
  </div>
</body>
</html>

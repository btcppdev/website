<!DOCTYPE html>
<html>
<head>
  <title>Link GitHub · bitcoin++</title>
  {{ block "header" . }} {{ end }}
</head>
<body class="btcpp-profile-edit-page">
  {{ block "mainnav" . }} {{ end }}
  <main class="profile-edit-shell person-emails-page">
    <div class="profile-edit-back"><a href="/dashboard/emails">← Account settings</a></div>
    <header class="profile-edit-hero">
      <p class="profile-edit-tag">§ SIGN-IN METHOD</p>
      <h1>Link <span>GitHub.</span></h1>
      <p>Confirm that GitHub @{{ .Identity.Username }} should sign in to the bitcoin++ profile for {{ .Speaker.Name }}.</p>
    </header>
    <section class="profile-edit-section is-open">
      <div class="profile-edit-section__index">GH</div>
      <div class="profile-edit-section__body">
        <div class="profile-edit-section__head"><div><p>§ VERIFIED BY GITHUB</p><h2>@{{ .Identity.Username }}</h2></div></div>
        <div class="person-email-list">
          <div class="person-email-row"><div><strong>GitHub account ID {{ .Identity.Subject }}</strong>{{ if .Identity.Email }}<small>{{ .Identity.Email }}{{ if .Identity.EmailVerified }} · verified{{ end }}</small>{{ end }}</div></div>
        </div>
        <form method="POST" action="/auth/oauth/github/confirm" class="person-email-add">
          <input type="hidden" name="csrf" value="{{ .CSRF }}">
          <button type="submit">Link GitHub to this profile</button>
          <a href="/dashboard/emails">Cancel</a>
        </form>
      </div>
    </section>
  </main>
  {{ block "footer" . }} {{ end }}
</body>
</html>

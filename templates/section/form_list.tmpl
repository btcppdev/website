{{ define "form_list" }}
<div class="btcpp-form-choice flex gap-x-4 sm:col-span-2 mt-2.5">
    <div class="flex h-6 items-center">
        <div class="btcpp-form-choice__control group relative inline-flex w-8 shrink-0 rounded-full bg-gray-200 p-px outline-offset-2 outline-bitcoin ring-1 ring-inset ring-gray-900/5 transition-colors duration-200 ease-in-out has-[:checked]:bg-bitcion has-[:focus-visible]:outline has-[:focus-visible]:outline-2">
            <input id="{{ .ItemID }}" type="checkbox" name="{{ .ItemID }}" aria-label="{{ .ItemID }}" class="text-bitcoin absolute inset-0 size-full appearance-none focus:outline-none"
            {{ if .Checked }}
                checked
            {{ end }}
            />
            <span class="btcpp-form-choice__box size-4 rounded-full bg-white shadow-sm ring-1 ring-gray-900/5 transition-transform duration-200 ease-in-out group-has-[:checked]:translate-x-3.5"></span>
        </div>
    </div>
    <label for="{{ .ItemID }}" class="text-sm/6 text-gray-600">
    {{ .ItemDesc }}
    </label>
</div>
{{ end }}
